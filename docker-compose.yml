services:
  portal:
    build: .
    image: portal
    container_name: portal
    restart: unless-stopped
    ports:
      - "127.0.0.1:8300:80"   # loopback för felsökning; utåt nås tjänsten via Caddy
    networks:
      - proxy

networks:
  proxy:
    external: true
