{
  "name": "portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "stats": "node scripts/generate-stats.mjs",
    "predev": "node scripts/generate-stats.mjs",
    "dev": "vite",
    "prebuild": "node scripts/generate-stats.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  }
}
